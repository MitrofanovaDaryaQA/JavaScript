/* Создайте функцию getSum, которая принимает три параметра a, b, c - целые числа и возвращает их сумму.

Вам не нужно вызывать данную функцию, только написать ее. */

function getSum(a,b,c){
     let result=a+b+c
     return(result);
};




